const Follow = ({userId, id}: {userId: string, id: string}) => {
  return (
    <button className="bg-btn-primary py-2 max-sm:rounded-xl rounded-sm max-sm:w-full sm:px-20">
        Follow
    </button>
  )
}

export default Follow
